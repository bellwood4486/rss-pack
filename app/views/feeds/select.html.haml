%h1 Select feed
.row
  .col-md-6.col-md-offset-3
    = @feeds.count
    - @feeds.each do |feed|
      = form_for(feed, url: feeds_path) do |f|
        = feed.title
        = feed.url
        = feed.content_type

        = f.submit '登録', class: "btn btn-primary"
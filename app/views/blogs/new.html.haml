%h1 New Feed
.row
  .col-md-6.col-md-offset-3
    = form_for(@blog, url: select_new_feed_path) do |f|
      = render 'shared/errors', model: @blog
      = f.label :url
      = f.text_field :url, class: 'form-control'
      = f.submit 'select', class: "btn btn-primary"

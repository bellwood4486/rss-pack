%h1= current_user.email
%span= link_to 'view my profile', current_user
%span= link_to "#{pluralize(current_user.feeds.count, 'feed')}", feeds_path
%span= text_field_tag 'rss_url', rss_url(current_user.packs.first.rss_token), readonly: true
%h1= 'Pack'

%p pack rss token : #{@pack.rss_token}

= link_to '追加', new_feed_path
%table.table.table-striped.table-bordered.table-hover.table-condensed
  %thead
    %tr
      %th TITLE
      %th URL
  %tbody
    -# TODO フラグメントに切り出し
    - @pack.feeds.each do |feed|
      %tr
        %td= feed.title
        %td= feed.url
        %td= link_to '削除', feed, method: :delete, data: { confirm: 'Are you sure?'}